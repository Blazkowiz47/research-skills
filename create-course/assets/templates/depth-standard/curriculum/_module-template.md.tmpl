# Module ID — Topic

When instantiating this template, give the module a unique order and move only the skill IDs it owns out of `M00-calibration.md`. Update the tracker and all marker lines together.

<!-- course:module-order=MODULE_ORDER -->
<!-- course:module-skills=OWNED_SKILL_ID_LIST -->
<!-- course:module-prerequisites=EXTERNAL_PREREQUISITE_ID_LIST -->

Replace the marker values before moving a copy into `curriculum/modules/`. The prerequisite marker must include every tracker dependency of an owned skill when that dependency is owned by another, earlier module. Do not list a skill the module itself owns as an entry prerequisite.

## Entry prerequisites

List prerequisite skill IDs and their required evidence level.

## Demonstrable outcomes

- Outcome:
- Skill IDs:

## Guided learning and practice

| Step | Prediction or retrieval | Practice | Evidence |
|---:|---|---|---|
| 1 |  |  |  |

## Exit gate

State the observable performance, conditions, independent check, and evidence link required to advance.
